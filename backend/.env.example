PORT=3000
JWT_SECRET=devsecret
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rrent_dev

# CORS Configuration
# For development, leave empty to use default localhost origins (http://localhost:3000, 5173, 3001)
# For production, set explicitly as comma-separated list
# Example: CORS_ALLOWED_ORIGINS=https://app.rrent.com,https://admin.rrent.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:3001

# Rate Limiting Configuration
# Login endpoint rate limit: max attempts per time window
LOGIN_RATE_LIMIT=5
# Time window in seconds
LOGIN_RATE_TTL=60
